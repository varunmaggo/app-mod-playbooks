apiVersion: v1
kind: Service
metadata:
  name: ${{ values.appName }}
  namespace: ${{ values.namespace }}
  labels:
    app: ${{ values.appName }}
spec:
  selector:
    app: ${{ values.appName }}
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
